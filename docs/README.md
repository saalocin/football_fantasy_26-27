# docs/ — hand-written reference only

Longer-form reference that would make `CLAUDE.md` too long to actually get read.
Nothing here is generated; generated output lives in `report/`.

Planned:

| file | owns | ticket |
| --- | --- | --- |
| `architecture.md` | the stage map, the command surface, which commands touch the network | FOO-23 |
| `source-onboarding.md` | how a source becomes legal to fetch; the verdict ladder | FOO-20 |
